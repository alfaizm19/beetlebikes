    <!--footer start-->
    <div class="footer">
        <div class="footer-top">
            <div class="container">
                <div class="row">
                    <div class="col-md-4 col-sm-6 col-xs-12">
                        <div class="single-footer footer-dec">
                            <div class="footer-logo">
                                <a href="<?php echo base_url();?>"><img src="<?php echo base_url();?>assets/front/img/logo/logo.png" width="225px" alt=""></a>
                            </div>
                            <p class="dec">
                            Gift your child the joy of cycling with Beetle Bikes. High quality bikes, proudly made with love in India.
                            </p>
                            <span class="fotr-img"><img src="<?php echo base_url();?>assets/front/img/logo/make_in_india.png" ></span>

                            
                            <div class="social-icon">
                                <a href="https://www.facebook.com/BeetleBikes/"><i class="fa fa-facebook"></i></a>
                                <a href="https://www.instagram.com/beetle_bikes_india/"><i class="fa fa-instagram"></i></a>
                                <a href="https://www.youtube.com/channel/UCy_Z7AemC7LwoVCo34-msZA"><i class="fa fa-youtube-play"></i></a>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-3 col-sm-6 col-xs-12">
                        <div class="single-footer">
                            <ul class="mainmenu">
                                <li><a href="<?php echo base_url();?>contact"><i class="fa fa-angle-right"></i> Contact Us</a></li>
                                <li><a href="<?php echo base_url();?>about"><i class="fa fa-angle-right"></i> About Us</a></li>
                                
                                <li><a href="<?php echo base_url();?>dealers-enquiry"><i class="fa fa-angle-right"></i> Dealer Enquiry</a></li>
                               
                                
                               
                            </ul>
                        </div>
                    </div>
                    <div class="col-md-3 col-sm-6 col-xs-12">
                        <div class="single-footer">
                            <ul class="mainmenu">
                               <li><a href="<?php echo base_url();?>privacy-policy"><i class="fa fa-angle-right"></i> Privacy policy</a></li>

                                <li><a href="<?php echo base_url();?>warranty-terms"><i class="fa fa-angle-right"></i> Warranty terms</a></li>
                                
                                <li><a target="_blank" href="<?php echo base_url('return-and-refund-policy') ?>"><i class="fa fa-angle-right"></i> Refund & Return Policy</a></li>
                                
                            </ul>
                        </div>
                    </div>

                    <style type="text/css">
                        .footer-content {
                            margin-top: 20px;
                            text-align: justify;
                        }
                        .footer-content hr {
                            border-top-color: transparent;
                            margin: 20px 0;
                        }

                        .footer-content h4 {
                            margin-top: 0;
                            text-transform: none;
                            font-weight: 500;
                            font-size: 1.2rem;
                        }

                        .footer-content ul {
                            margin-bottom: 0;
                        }

                        .list-unstyled {
                            padding-left: 0;
                            list-style: none;
                        }

                        .footer-content ul li {
                            display: inline-block;
                            margin-right: 10px;
                            position: relative;
                            padding-left: 10px;
                            color: #fff;
                        }

                        .shopNow ul li a {
                            color: #fff;
                        }

                        .shopNow h5 {
                            margin-top: 0;
                            color: #fff;
                            text-transform: none;
                            font-weight: 500;
                            font-size: 1.3rem;
                        }

                        .shopNow ul {
                            margin-bottom: 30px;
                        }

                        .brandPromise ul li:after {
                            content: "";
                            position: absolute;
                            top: 50%;
                            transform: translateY(-50%);
                            -webkit-transform: translateY(-50%);
                            left: 0;
                            width: 4px;
                            height: 4px;
                            background: white;
                            border-radius: 50%;
                        }

                        .shopNow ul li:after {
                            content: "";
                            position: absolute;
                            top: 50%;
                            transform: translateY(-50%);
                            -webkit-transform: translateY(-50%);
                            left: 0;
                            width: 4px;
                            height: 4px;
                            background: white;
                            border-radius: 50%;
                        }
                    </style>

                    <div class="col-md-2 col-sm-6 col-xs-12">
                        <div class="single-footer">
                            <ul class="mainmenu">
                                
                                 
                                <li><a href="<?php echo base_url('media');?>"><i class="fa fa-angle-right"></i> Media</a></li>
                                <li><a href="<?php echo base_url('faq');?>"><i class="fa fa-angle-right"></i> FAQs</a></li>

                            </ul>
                        </div>
                    </div>                    

                    <div class="col-md-12 footer-content">
                        <p>Beetle Bikes is a premium kids’ cycle brand that promises to offer high quality kids’ cycles and cycling accessories to customers. Beetle Bikes is launched by avid cyclists, Yogesh & Himanshu, who have done more than 50000 km of cycling between them. With their extensive experience and love for cycling, they have ensured that the products are designed to meet all needs of a cyclist. You can now order a cycle online on our website and get the cycle delivered at the comfort of your home. You can also get the cycle assembled with our highly trained technicians, who will come at the appointed time to your home and assemble the cycle for you. You can also choose from a range of accessories like cycling helmets, cycling lights, cycling bottles, cycle locks and more on our website. Check out our blogs section to know more about cycle sizes, types of cycles, saddle height adjustment, fixing basic issues like punctures, and about cycling accessories. Wheely Bikes are also available on the website.</p>

                        <hr>

                        <div class="brandPromise">
                            <h4>Why Beetle Bikes:</h4>
                            <p>High quality cycles | European style | Life time warranty on frames | Home delivery of cycles | Assembly service at home for cycles | Easy EMI options | Safe & Secure payments | Designed by cyclists | Made in India | Buy cycle online </p>
                        </div>
                        <hr class="lighter">

                        <div class="brandPromise">
                            <h4>Cycle categories:</h4>
                            <p>Kids Cycles | MTB | Hybrid cycles | Road cycles | E Bikes </p>
                        </div>
                        <hr class="lighter">

                        <div class="brandPromise">
                            <h4>Cycles for:</h4>
                            <p>Kids | Boys | Girls | Men | Women | Unisex Cycles</p>
                        </div>
                        <hr class="lighter">

                        <div class="brandPromise">
                            <h4>Cycles Sizes:</h4>
                            <p>12T | 16T | 20T | 24T | 26T | 27.5T | 29T | Balance Bikes</p>
                        </div>

                        <hr class="lighter">

                        <div class="brandPromise">
                            <h4>Accessories:</h4>
                            <p>Cycling bottles | Cycling lights | Kids Helmets for Cycling | Cycling spares & spare parts | Cycling components </p>
                        </div>

                        <hr class="lighter">

                        <div class="brandPromise">
                            <h4>Brands:</h4>
                            <p>Beetle Bikes | Wheely Bikes</p>
                        </div>

                        <hr class="lighter">

                        <div class="brandPromise">
                            <h4>SKUs:</h4>
                            <p>Beetle Toddler | Beetle Candy | Beetle Sprinkles | Beetle Bubblegum | Beetle Cruise | Beetle Panache | Beetle Storm | Beetle Vintage | Beetle Shadow | Beetle Black | Wheely Zest</p>
                        </div>

                    </div>
                </div>
            </div>
        </div>
        <div class="footer-botton">
            <div class="container">
                <div class="row">
                    <div class="col-md-6 col-xs-12 col-sm-12">
                        <p>Copyright © Movetime Technologies Pvt Ltd. 2021.</p>
                    </div>
                    <div class="col-md-6 col-xs-12 col-sm-12">
                       <div class="master-card">
                          <img src="<?php echo base_url();?>assets/front/img/logo/cont.png" alt="">
                       </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!--footer-area end-->
    
    <!-- Placed js at the end of the document so the pages load faster -->

    <!-- All js plugins included in this file. -->
    
    <script src="<?php echo base_url();?>assets/front/js/bootstrap.min.js"></script>

    <!-- <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script> -->

    <script src="<?php echo base_url();?>assets/front/js/jquery.nivo.slider.pack.js"></script>
    <script src="<?php echo base_url();?>assets/front/js/owl.carousel.min.js"></script>
    <script src="<?php echo base_url();?>assets/front/js/mail.js"></script>
    <script src="<?php echo base_url();?>assets/front/js/plugins.js"></script>
    <script src="<?php echo base_url();?>assets/front/js/active.js?ver=1"></script>

    <script type="text/javascript">        

        $(document).on('click', '.cart_add_quantity', function(e) {
            var productid = $(this).attr("data-productid");            
            $.ajax({
                  url: "<?php echo base_url('products/addCartProduct') ?>",
                  data: {
                    '<?php echo $this->security->get_csrf_token_name(); ?>' : '<?php echo $this->security->get_csrf_hash(); ?>', 
                    product_id : productid,
                    cart_product_qty : $("#qty_"+productid).val(),
                  },
                  type: "POST",
                  dataType: "JSON",
                  cache: false,
                  success: function(response){
                    if(response.stock){
                        //showMessage('danger', `Quantity avalaible only ${response.stock}`);
                        showMessage('danger', `Quantity avalaible only ${response.stock}`);
                        return false;
                    }
                    if(response.status == 'success'){
                        fbq('track', 'AddToCart');
                        $(".cart_product_"+productid).css("display","none");
                        $(".remove_cart_"+productid).css("display","inline");   
                        jQuery('#head_cart').html(response.cartList);
                        jQuery('#headCartCount').html(response.cartCount); 
                        getCartData(productId = '',qty= '');
                        getHeaderCartData();

                        dataLayer.push({ ecommerce: null });  // Clear the previous ecommerce object.
                        dataLayer.push({
                          event: "add_to_cart",
                          ecommerce: response.dataLayer
                        });

                    }
                  },

                  error: function (xhr, ajaxOptions, thrownError) {
                    // showMessage('danger', `Something went wrong please try again`);
                    showMessage('danger', 'Something went wrong please try again');
                  }
            });
           
        }); 

        $(document).on('click', '.cart_buy_now_quantity', function(e) {
            var productid = $(this).attr("data-productid");            
            $.ajax({
                  url: "<?php echo base_url('products/addCartProduct') ?>",
                  data: {
                    '<?php echo $this->security->get_csrf_token_name(); ?>' : '<?php echo $this->security->get_csrf_hash(); ?>', 
                    product_id : productid,
                    cart_product_qty : $("#qty_"+productid).val(),
                  },
                  type: "POST",
                  dataType: "JSON",
                  cache: false,
                  success: function(response){
                    if(response.stock){
                        //showMessage('danger', `Quantity avalaible only ${response.stock}`);
                        showMessage('danger', `Quantity avalaible only ${response.stock}`);
                        return false;
                    }
                    if(response.status == 'success'){
                        fbq('track', 'AddToCart');
                        window.location.href = "<?php echo base_url('cart'); ?>"
                        // $(".cart_product_"+productid).css("display","none");
                        // $(".remove_cart_"+productid).css("display","inline");   
                        // jQuery('#head_cart').html(response.cartList);
                        // jQuery('#headCartCount').html(response.cartCount); 
                        // getCartData(productId = '',qty= '');
                        // getHeaderCartData();

                        dataLayer.push({ ecommerce: null });  // Clear the previous ecommerce object.
                        dataLayer.push({
                          event: "add_to_cart",
                          ecommerce: response.dataLayer
                        });
                    }
                  },

                  error: function (xhr, ajaxOptions, thrownError) {
                    //showMessage('danger', `Something went wrong please try again`);
                    showMessage('danger', `Something went wrong please try again`);
                  }
            });
           
        });        

        $(document).on('click', '.cart_add_signle', function(e) {
            var productid = $(this).attr("data-productid");            
            $.ajax({
                  url: "<?php echo base_url('products/addCartProduct') ?>",
                  data: {
                    '<?php echo $this->security->get_csrf_token_name(); ?>' : '<?php echo $this->security->get_csrf_hash(); ?>', 
                    product_id : productid,
                  },
                  type: "POST",
                  dataType: "JSON",
                  cache: false,
                  success: function(response){
                    if(response.stock){
                        showMessage('danger', `Quantity avalaible only ${response.stock}`);
                        return false;
                    }
                    if(response.status == 'success'){
                        fbq('track', 'AddToCart');
                        $(".cart_product_"+productid).css("display","none");
                        $(".remove_cart_"+productid).css("display","block");
                        jQuery('#head_cart').html(''); 
                        jQuery('#headCartCount').html(response.cartCount); 
                        if(response.cartList){
                            jQuery('#head_cart').html(response.cartList);
                        }

                        dataLayer.push({ ecommerce: null });  // Clear the previous ecommerce object.
                        dataLayer.push({
                          event: "add_to_cart",
                          ecommerce: response.dataLayer
                        });

                    }
                  },

                  error: function (xhr, ajaxOptions, thrownError) {
                    showMessage('danger', `Something went wrong please try again`);
                  }
            });
           
        });

        $(document).on('click', '.cart_add_signle-cart-page', function(e) {
            var productid = $(this).attr("data-productid");            
            $.ajax({
                  url: "<?php echo base_url('products/addCartProduct') ?>",
                  data: {
                    '<?php echo $this->security->get_csrf_token_name(); ?>' : '<?php echo $this->security->get_csrf_hash(); ?>', 
                    product_id : productid,
                  },
                  type: "POST",
                  dataType: "JSON",
                  cache: false,
                  success: function(response){
                    if(response.stock){
                        showMessage('danger', `Quantity avalaible only ${response.stock}`);
                        return false;
                    }
                    if(response.status == 'success'){
                        fbq('track', 'AddToCart');
                        $(".cart_product_"+productid).css("display","none");
                        $(".remove_cart_"+productid).css("display","block");
                        jQuery('#head_cart').html(''); 
                        jQuery('#headCartCount').html(response.cartCount); 
                        if(response.cartList){
                            jQuery('#head_cart').html(response.cartList);
                        }

                        dataLayer.push({ ecommerce: null });  // Clear the previous ecommerce object.
                        dataLayer.push({
                          event: "add_to_cart",
                          ecommerce: response.dataLayer
                        });

                        location.reload();

                    }
                  },

                  error: function (xhr, ajaxOptions, thrownError) {
                    showMessage('danger', `Something went wrong please try again`);
                  }
            });
           
        });

        $(document).on('click', '.remove_cart_product', function(e) {
            var productid = $(this).attr("data-productid");            
            $.ajax({
                  url: "<?php echo base_url('products/removeCartProduct') ?>",
                  data: {
                    '<?php echo $this->security->get_csrf_token_name(); ?>' : '<?php echo $this->security->get_csrf_hash(); ?>', 
                    product_id : productid,
                  },
                  type: "POST",
                  dataType: "json",
                  cache: false,
                  success: function(response){

                    if(response.status == 'success'){
                        jQuery('#head_cart').html(''); 
                        jQuery('#headCartCount').html(response.cartCount); 
                        if(response.cartList){
                            jQuery('#head_cart').html(response.cartList);
                            getCartData(productId = '',qty= '');
                        }
                    }
                  },
                   error: function (xhr, ajaxOptions, thrownError) {
                    showMessage('danger', `Something went wrong please try again`);
                  }
            });
           
        });
        
        $(document).on('click', '.wishlist_add', function(e) {
            var productid = $(this).attr("data-productid");            
            $.ajax({
                  url: "<?php echo base_url('products/addWishlistProduct') ?>",
                  data: {
                    '<?php echo $this->security->get_csrf_token_name(); ?>' : '<?php echo $this->security->get_csrf_hash(); ?>', 
                    product_id : productid,
                  },
                  type: "POST",
                  dataType: "json",
                  cache: false,
                  success: function(response){
                    if(response.status == 'success'){
                       $(".wishlist_add_"+productid).css("display","none");
                       $(".wishlist_remove_"+productid).css("display","block");
                        if( $('#wishlistCount').length )         // use this if you are using id to check
                        {
                            $('#wishlistCount').html('('+response.count+')');
                        }
                    }
                  },
                  error: function (xhr, ajaxOptions, thrownError) {
                    showMessage('danger', `Something went wrong please try again`);
                  }
            });
           
        });

        $(document).on('click', '.wishlist_remove', function(e) {
            var productid = $(this).attr("data-productid");            
            $.ajax({
                  url: "<?php echo base_url('products/removeWishlistProduct') ?>",
                  data: {
                    '<?php echo $this->security->get_csrf_token_name(); ?>' : '<?php echo $this->security->get_csrf_hash(); ?>', 
                    product_id : productid,
                  },
                  type: "POST",
                  dataType: "json",
                  cache: false,
                  success: function(response){
                    if(response.status == 'success'){
                       $(".wishlist_remove_"+productid).css("display","none");
                       $(".wishlist_add_"+productid).css("display","block");
                    }
                  },
                  error: function (xhr, ajaxOptions, thrownError) {
                    showMessage('danger', `Something went wrong please try again`);
                  }
            });
           
        });

       $("#checkPincodeBtn").on( "click", function() { 
            if($("#pincode").val()){ 
                $(".pincodeError").html('');                   
                $.ajax({
                      url: "<?php echo base_url('ajax/checkPincode') ?>",
                      data: {
                        '<?php echo $this->security->get_csrf_token_name(); ?>' : '<?php echo $this->security->get_csrf_hash(); ?>', 
                        pincode : $("#pincode").val(),
                      },
                      type: "POST",
                      dataType: "json",
                      cache: false,
                      success: function(response){
                        if(response.status == 'success'){
                           $(".pincodeSuccess").html('Delivery avalaible in '+response.delivery_days+' days');
                        }
                      },
                      error: function (xhr, ajaxOptions, thrownError) {
                        showMessage('danger', `Something went wrong please try again`);
                      }
                });
            } else{
                $(".pincodeError").html('Please enter pincode');
            }    
           
        });

        <?php 
    
        //if ($this->router->fetch_class() == 'product' && $this->router->fetch_method() == 'cart') { 
        ?>
            getCartData(productId = '',qty= '');

            function getHeaderCartData(){
                 $.ajax({
                  url: "<?php echo base_url() ?>products/cartHeader",
                  data: {
                    '<?php echo $this->security->get_csrf_token_name(); ?>' : '<?php echo $this->security->get_csrf_hash(); ?>', 
                  },
                  type: "POST",
                  dataType: "json",
                  cache: false,
                  success: function(response){
                        jQuery('#head_cart').html(''); 
                        jQuery('#headCartCount').html(response.cartCount); 
                        if(response.cartList){
                            jQuery('#head_cart').html(response.cartList);
                        }   
                  },
                  error: function (xhr, ajaxOptions, thrownError) {
                   // showMessage('danger', `Something went wrong please try again`);
                  }
                });
            }

            function getCartData(productId ,qty){
                if(productId){
                   var cardStatus = 1;
                } else {
                     var cardStatus = 0;
                }

                $.ajax({
                  url: "<?php echo base_url() ?>products/cartUpdate",
                  data: {
                    '<?php echo $this->security->get_csrf_token_name(); ?>' : '<?php echo $this->security->get_csrf_hash(); ?>', 
                    productId:productId,
                    qty : qty,
                    updateCart : cardStatus,
                  },
                  type: "POST",
                  dataType: "html",
                  cache: false,
                  success: function(response){
                    if(response){
                       $("#cartPage").html(response);
                       getHeaderCartData();                   
                       //getCartData(productId = '',qty= '');
                    }
                  },
                  error: function (xhr, ajaxOptions, thrownError) {
                    //showMessage('danger', `Something went wrong please try again`);
                  }
                });
            }

            $(document).on('click', '.qtybutton', function(e) {

                var productId = $(this).data('productid');
                var qty = $("#updateQty_"+productId).val();
                getCartData(productId,qty);
            });    
        <?php
            //}
        ?>  
        <?php 
            if ($this->router->fetch_class() == 'blog' && $this->router->fetch_method() == 'index') { 
        ?>
            var page = 0;

            getBlogList(page);

            function getBlogList(page){


                $.ajax({
                      url: "<?php echo base_url() ?>blogListAjax/"+page,
                      data: {
                        '<?php echo $this->security->get_csrf_token_name(); ?>' : '<?php echo $this->security->get_csrf_hash(); ?>', 
                        page:page,
                      },
                      type: "POST",
                      dataType: "html",
                      // dataType: "json",
                      cache: false,
                      success: function(response){
                        if(response){
                           $("#ajaxBlogContent").html(response);
                            
                        }
                      },
                      error: function (xhr, ajaxOptions, thrownError) {
                        showMessage('danger', `Something went wrong please try again`);
                      }
                });
            }

           function getPaginationSegment(){
                var href = $('.pagination li.active a').attr('href');
                if(href){
                    var parts = href.split('/');
                    var lastSegment = parts.pop() || parts.pop();  // handle potential trailing slash
                    if(lastSegment){
                        return lastSegment;
                    } else {
                        return 0;
                    }
                }
                
            }

       
            $(document).on('click', '.pagination li a', function(e) {
                event.preventDefault();
                var page = $(this).data('ci-pagination-page');
                var href = $(this).attr('href');

                if(href != 'javascript:void(0)'){
                    if(page){
                        var parts = href.split('/');
                        var lastSegment = parts.pop() || parts.pop();  // handle potential trailing slash

                        if(lastSegment){
                            getBlogList(lastSegment);
                            $([document.documentElement, document.body]).animate({
                                scrollTop: $("#ajaxBlogContent").offset().top
                            }, 1000);
                        } 
                    }
                }
                
               
            });

        <?php
            }
        ?>  

        <?php if ($this->uri->segment(1) == 'category' && $this->uri->segment(2) != ''):
        ?>

            var page = 0;

            getProductFilter(page);

            function getProductFilter(page){

            var catFilter = []; 
            $(".catFilter:checked").each(function(){
                catFilter.push($(this).val());
            });

            var attributesFilterData = []; 
            $(".attributeFilter:checked").each(function(){
                attributesFilterData.push($(this).val());
            });

            myParentCatID = "<?php echo $this->session->userdata('subCategoryId'); ?>";

            if (catFilter.length < 1) {
                catFilter = [myParentCatID];
            }
           

            currentTab = getProductCurrentTab();
            price = [];
            var minPrice = $("#min-price").val();
            var maxPrice = $("#max-price").val();

            price.push(minPrice.replace("₹", ""));
            price.push(maxPrice.replace("₹", ""));            

            $.ajax({
              url: "<?php echo base_url() ?>productsAjax/"+page,
              data: {
                '<?php echo $this->security->get_csrf_token_name(); ?>' : '<?php echo $this->security->get_csrf_hash(); ?>', 
                page:page,
                productName : $("#productSearchText").val(),
                sort : $("#productOrderBy").val(),
                perPage : $("#productOnPage").val(),
                price : price.toString(),
                category : catFilter.toString(),
                attribute : attributesFilterData.toString(),
              },
              type: "POST",
              dataType: "html",
              // dataType: "json",
              cache: false,
              success: function(response){
                if(response){
                   $("#ajaxProductFilter").html(response);
                    if(currentTab == 'listView'){
                       $("#productTabList").attr("class","active");
                       $("#list-view").attr("class","tab-pane active");
                       $("#productTabGrid").removeClass("active");
                       $("#grid-view").removeClass("active");
                       $('#productTabList a').attr("aria-expanded","true");
                       $('#productTabGrid a').attr("aria-expanded","false");
                    } 
                }
              },
              error: function (xhr, ajaxOptions, thrownError) {
                showMessage('danger', `Something went wrong please try again`);
              }
                });
            }

        
            function getPaginationSegment(){
                var href = $('.pagination li.active a').attr('href');
                if(href){
                    var parts = href.split('/');
                    var lastSegment = parts.pop() || parts.pop();  // handle potential trailing slash
                    if(lastSegment){
                        return lastSegment;
                    } else {
                        return 0;
                    }
                }
                
            }

           
            $(document).on('click', '.pagination li a', function(e) {
                event.preventDefault();
                var page = $(this).data('ci-pagination-page');
                var href = $(this).attr('href');

                if(href != 'javascript:void(0)'){
                    if(page){
                        var parts = href.split('/');
                        var lastSegment = parts.pop() || parts.pop();  // handle potential trailing slash

                        if(lastSegment){
                            getProductFilter(lastSegment);
                            $([document.documentElement, document.body]).animate({
                                scrollTop: $(".shop-item-filter").offset().top
                            }, 1000);
                        } 
                    }
                }
                
               
            });

            $(document).on('change', '#productOrderBy', function(e) {
                lastSegment = getPaginationSegment();
                
                if(lastSegment != 'javascript:void(0)'){
                    getProductFilter(lastSegment);
                        return lastSegment;
                } else {
                    getProductFilter(0);
                }
            });

            $(document).on('change', '#productOnPage', function(e) {
                lastSegment = getPaginationSegment();
                getProductFilter(0);
            });
            

            $( "#price-slider" ).slider({
                slide: function(event, ui) {
                    $( "#min-price" ).val('₹' + ui.values[ 0 ] );
                    $( "#max-price" ).val('₹' + ui.values[ 1 ] );
                    getProductFilter(0);
                }
            });

            function productSideFilter() {
                getProductFilter(0);
            }

            function attributeFilter() {
                getProductFilter(0);
            }

        <?php endif; ?>

        <?php 
        
        if ($this->router->fetch_class() == 'product' && $this->router->fetch_method() == 'index') { 
        ?>
     
        var page = 0;

        getProductFilter(page);

        function getProductFilter(page){

            var catFilter = []; 
            $(".catFilter:checked").each(function(){
                catFilter.push($(this).val());
            });

            var attributesFilterData = []; 
            $(".attributeFilter:checked").each(function(){
                attributesFilterData.push($(this).val());
            });
           

            currentTab = getProductCurrentTab();
            price = [];
            var minPrice = $("#min-price").val();
            var maxPrice = $("#max-price").val();

            price.push(minPrice.replace("₹", ""));
            price.push(maxPrice.replace("₹", ""));

            $.ajax({
              url: "<?php echo base_url() ?>productsAjax/"+page,
              data: {
                '<?php echo $this->security->get_csrf_token_name(); ?>' : '<?php echo $this->security->get_csrf_hash(); ?>', 
                page:page,
                productName : $("#productSearchText").val(),
                sort : $("#productOrderBy").val(),
                perPage : $("#productOnPage").val(),
                price : price.toString(),
                category : catFilter.toString(),
                attribute : attributesFilterData.toString(),
              },
              type: "POST",
              dataType: "html",
              // dataType: "json",
              cache: false,
              success: function(response){
                if(response){
                   $("#ajaxProductFilter").html(response);
                    if(currentTab == 'listView'){
                       $("#productTabList").attr("class","active");
                       $("#list-view").attr("class","tab-pane active");
                       $("#productTabGrid").removeClass("active");
                       $("#grid-view").removeClass("active");
                       $('#productTabList a').attr("aria-expanded","true");
                       $('#productTabGrid a').attr("aria-expanded","false");
                    } 
                }
              },
              error: function (xhr, ajaxOptions, thrownError) {
                showMessage('danger', `Something went wrong please try again`);
              }
            });
        }

    
        function getPaginationSegment(){
            var href = $('.pagination li.active a').attr('href');
            if(href){
                var parts = href.split('/');
                var lastSegment = parts.pop() || parts.pop();  // handle potential trailing slash
                if(lastSegment){
                    return lastSegment;
                } else {
                    return 0;
                }
            }
            
        }

       
        $(document).on('click', '.pagination li a', function(e) {
            event.preventDefault();
            var page = $(this).data('ci-pagination-page');
            var href = $(this).attr('href');

            if(href != 'javascript:void(0)'){
                if(page){
                    var parts = href.split('/');
                    var lastSegment = parts.pop() || parts.pop();  // handle potential trailing slash

                    if(lastSegment){
                        getProductFilter(lastSegment);
                        $([document.documentElement, document.body]).animate({
                            scrollTop: $(".shop-item-filter").offset().top
                        }, 1000);
                    } 
                }
            }
            
           
        });

        $(document).on('change', '#productOrderBy', function(e) {
            lastSegment = getPaginationSegment();
            
            if(lastSegment != 'javascript:void(0)'){
                getProductFilter(lastSegment);
                    return lastSegment;
            } else {
                getProductFilter(0);
            }
        });

        $(document).on('change', '#productOnPage', function(e) {
            lastSegment = getPaginationSegment();
            getProductFilter(0);
        });
        

        $( "#price-slider" ).slider({
            slide: function(event, ui) {
                $( "#min-price" ).val('₹' + ui.values[ 0 ] );
                $( "#max-price" ).val('₹' + ui.values[ 1 ] );
                getProductFilter(0);
            }
        });

        function productSideFilter() {
            getProductFilter(0);
        }

        function attributeFilter() {
            getProductFilter(0);
        }



    <?php
        }
    ?>
    $(document).on('click', '#couponBtn', function(e) { 
        event.preventDefault();
        var couponCode = $('#couponCode').val();
        if(couponCode){
            $('#couponCodeError').html('');
             $.ajax({
                  url: "<?php echo base_url('ajax/apply_coupon') ?>",
                  data: {
                    '<?php echo $this->security->get_csrf_token_name(); ?>' : '<?php echo $this->security->get_csrf_hash(); ?>', 
                    couponCode : couponCode,
                  },
                  type: "POST",
                  dataType: "json",
                  cache: false,
                  beforeSend: function() {
                    $("#couponCodeError").css('color', 'red');
                  },
                  success: function(res){
                    if(res.error == true) {
                        if (res.type == 'field') {
                            $.each(res, function(index, val) {
                                $("#"+index+"Error").html(val)
                            });
                        }

                        if (res.type == 'final') {
                            $('#couponCodeError').html(res.msg);
                        }

                        if (res.type == 'login') {

                            $('#couponCodeError').html(res.msg);   

                            setTimeout(function() {
                                window.location.href = res.url;
                            }, 3000);
                        }
                    } else {
                        $("#couponCodeError").css('color', 'green');
                        $("#couponCodeError").html(res.msg);

                        $('.my-cart-subtotal').html(res.summary);
                        $('.my-cart-total').html(res.summary2);

                        //getCartData(productId = '',qty= '');
                        getHeaderCartData();
                    }
                    // if(response.status == 'success'){
                    //     location.reload();
                    // } else {
                    //     $('#couponCodeError').html('Coupon code not valid');
                    // }
                  },
                  error: function (xhr, ajaxOptions, thrownError) {
                    showMessage('danger', `Something went wrong please try again`);
                  }
            });
        } else {
            $('#couponCodeError').html('Please enter coupon code');
        }
    });

    function quantityNumberOnly(evt){
        var charCode = (evt.which) ? evt.which : evt.keyCode
        if (charCode > 31 && (charCode < 48 || charCode > 57))
            return false;
        return true;
    }

    <?php 
        if ($this->router->fetch_class() == 'category' && $this->router->fetch_method() == 'subcategory') { 
    ?>
       //  var page = 0;

       //  getSubCategoriesFilter(page);

       //  function getSubCategoriesFilter(page){

       //      currentTab = getProductCurrentTab();

       //      $.ajax({
       //            url: "<?php echo base_url() ?>subCategoryListAjax/"+page,
       //            data: {
       //              '<?php echo $this->security->get_csrf_token_name(); ?>' : '<?php echo $this->security->get_csrf_hash(); ?>', 
       //              page:page,
       //              perPage : $("#CategoiresOnPage").val(),
       //              catId : <?php echo $this->session->userdata('subCategoryId'); ?>,
       //            },
       //            type: "POST",
       //            dataType: "html",
       //            // dataType: "json",
       //            cache: false,
       //            success: function(response){
       //              if(response){
       //                 $("#ajaxSubCategoiresFilter").html(response);
       //                  if(currentTab == 'listView'){
       //                     $("#categoryTabList").attr("class","active");
       //                     $("#list-view").attr("class","tab-pane active");
       //                     $("#categoryTabGrid").removeClass("active");
       //                     $("#grid-view").removeClass("active");
       //                     $('#categoryTabList a').attr("aria-expanded","true");
       //                     $('#categoryTabGrid a').attr("aria-expanded","false");
       //                  } 
       //              }
       //            },
       //            error: function (xhr, ajaxOptions, thrownError) {
       //              showMessage('danger', `Something went wrong please try again`);
       //            }
       //      });
       //  }

       // function getPaginationSegment(){
       //      var href = $('.pagination li.active a').attr('href');
       //      if(href){
       //          var parts = href.split('/');
       //          var lastSegment = parts.pop() || parts.pop();  // handle potential trailing slash
       //          if(lastSegment){
       //              return lastSegment;
       //          } else {
       //              return 0;
       //          }
       //      }
                
       //  }

       //  $(document).on('click', '.pagination li a', function(e) {
       //      event.preventDefault();
       //      var page = $(this).data('ci-pagination-page');
       //      var href = $(this).attr('href');

       //      if(href != 'javascript:void(0)'){
       //          if(page){
       //              var parts = href.split('/');
       //              var lastSegment = parts.pop() || parts.pop();  // handle potential trailing slash

       //              if(lastSegment){
       //                  getSubCategoriesFilter(lastSegment);
       //                  $([document.documentElement, document.body]).animate({
       //                      scrollTop: $("#ajaxSubCategoiresFilter").offset().top
       //                  }, 1000);
       //              } 
       //          }
       //      }
            
           
       //  });


       //  $(document).on('change', '#subCategoiresOnPage', function(e) {
       //      getSubCategoriesFilter(0);
       //  });

    <?php
        }
    ?> 
    <?php 
        if ($this->router->fetch_class() == 'category' && $this->router->fetch_method() == 'index') { 
    ?>
        var page = 0;

        getCategoriesFilter(page);

        function getCategoriesFilter(page){

            currentTab = getProductCurrentTab();

            $.ajax({
                  url: "<?php echo base_url() ?>categoriesAjax/"+page,
                  data: {
                    '<?php echo $this->security->get_csrf_token_name(); ?>' : '<?php echo $this->security->get_csrf_hash(); ?>', 
                    page:page,
                    perPage : $("#CategoiresOnPage").val(),
                  },
                  type: "POST",
                  dataType: "html",
                  // dataType: "json",
                  cache: false,
                  success: function(response){
                    if(response){
                       $("#ajaxCategoiresFilter").html(response);
                        if(currentTab == 'listView'){
                           $("#categoryTabList").attr("class","active");
                           $("#list-view").attr("class","tab-pane active");
                           $("#categoryTabGrid").removeClass("active");
                           $("#grid-view").removeClass("active");
                           $('#categoryTabList a').attr("aria-expanded","true");
                           $('#categoryTabGrid a').attr("aria-expanded","false");
                        } 
                    }
                  },
                  error: function (xhr, ajaxOptions, thrownError) {
                    //alert('cccSomething went wrong please try again');
                    showMessage('danger', 'Something went wrong please try again');
                  }
            });
        }

        function getPaginationSegment(){
            var href = $('.pagination li.active a').attr('href');
            if(href){
                var parts = href.split('/');
                var lastSegment = parts.pop() || parts.pop();  // handle potential trailing slash
                if(lastSegment){
                    return lastSegment;
                } else {
                    return 0;
                }
            }
                
        }

        $(document).on('click', '.pagination li a', function(e) {
            event.preventDefault();
            var page = $(this).data('ci-pagination-page');
            var href = $(this).attr('href');

            if(href != 'javascript:void(0)'){
                if(page){
                    var parts = href.split('/');
                    var lastSegment = parts.pop() || parts.pop();  // handle potential trailing slash

                    if(lastSegment){
                        getCategoriesFilter(lastSegment);
                        $([document.documentElement, document.body]).animate({
                            scrollTop: $("#ajaxSubCategoiresFilter").offset().top
                        }, 1000);
                    } 
                }
            }
            
           
        });

        $(document).on('change', '#CategoiresOnPage', function(e) {
            getCategoriesFilter(0);
        });

    <?php
        }
    ?> 

    // Current tab for products and cateories
    function getProductCurrentTab(){
        var productTabList = $('#productTabList').attr('class');
        
        if(productTabList){
           var currentTab = 'listView';
        } else {
            var currentTab = 'gridView';
        }

        return currentTab;
    }

    <?php 
        if ($this->router->fetch_class() == 'profile') { 
    ?>
        $('#addaddress').click(function() {
            $('.append-form').show(2000);
            /*
                $('.form_submit').click(function(event) {
                    event.preventDefault();
                    $('.append-form').hide(2000);
                });
            */
        });
           
        $('.edit-form').click(function() {
            $('.append-form').show(2000);
            $('.form_submit').click(function(event) {
                event.preventDefault();
                $('.append-form').hide(2000);
            });
        });

        $(document).on('click', '#editProfile', function(e) {
            $('#profileArea').removeClass('disabledEdit');
            $("#profileBtn").removeAttr("disabled");
        });
            
    <?php
        }
    ?> 

    $(document).on('click', '#termsConditionCheckout', function(e) {
        if($('#termsConditionCheckout').prop("checked") == true){
            $("#rzp-button1").removeAttr("disabled");
        } else{
            $("#rzp-button1").prop('disabled', true);
        }
        
    });

    </script>
   
    <?php 
        if ($this->router->fetch_class() == 'home' && $this->router->fetch_method() == 'contact') { 
    ?>
        <!-- Google Map js -->
    <script src="https://maps.googleapis.com/maps/api/js"></script>
    <script>
        function initialize() {
          var mapOptions = {
            zoom: 15,
            scrollwheel: false,
            center: new google.maps.LatLng(23.765844, 90.358361)
          };

          var map = new google.maps.Map(document.getElementById('googleMap'),
              mapOptions);


          var marker = new google.maps.Marker({
            position: map.getCenter(),
            animation:google.maps.Animation.BOUNCE,
            map: map
          });

        }

        google.maps.event.addDomListener(window, 'load', initialize);
    </script>
    <?php
        }
    ?> 
    <?php 
       // if ($this->router->fetch_class() == 'checkout') { 
          
    ?>
    <script src="https://checkout.razorpay.com/v1/checkout.js"></script>
  

     <script>
        
        $(document).on('click', '#rzp-button1', function(e) {
            $('.error').html('');
            $.ajax({
                  url: "<?php echo base_url() ?>checkout/createRazorPayOrder",
                  data: $("#checkoutBillingForm").serialize(),//only input
                  type: "POST",
                  dataType: "json",
                  cache: false,
                  beforeSend: function() {
                       $("#rzp-button1").prop('disabled', true);
                  },
                  success: function(res){

                    if(res.status == 'success'){

                        var dataRazorpayOrder = res.data;

                        console.log(dataRazorpayOrder);

                       var options = {
                            "key": dataRazorpayOrder.key,
                            "amount": dataRazorpayOrder.amount,
                            "currency": "INR",
                            "name": "Beetle Bikes",
                            "description": "Premium Kids Bikes",
                            "image": "<?php echo base_url('assets/front/img/payment-logo.png') ?>",
                            "order_id": dataRazorpayOrder.order_id,
                            "callback_url": dataRazorpayOrder.callback_url,
                            "prefill": {
                                "name": dataRazorpayOrder.prefill.name,
                                "email": dataRazorpayOrder.prefill.email,
                                "contact": dataRazorpayOrder.prefill.contact
                            },
                            "theme": {
                                "color": "#cfa04f"
                            }
                        };

                        trigger_razorpay(options);
                        $("#rzp-button1").removeAttr('disabled');
                    } else if(res.status == 'fail'){

                        $("#rzp-button1").removeAttr("disabled");
                        if(res.message){
                            //alert(res.message);
                            showMessage('danger', res.message);
                            return false;
                        }
                       
                        if(res.billingAddressError.billing_first_name){
                            $('#error_billing_first_name').html(res.billingAddressError.billing_first_name);
                        }
                        if(res.billingAddressError.billing_last_name){
                             $('#error_billing_last_name').html(res.billingAddressError.billing_last_name);
                        }
                        if(res.billingAddressError.billing_email){
                             $('#error_billing_email').html(res.billingAddressError.billing_email);
                        }  
                        if(res.billingAddressError.billing_phone){
                             $('#error_billing_phone').html(res.billingAddressError.billing_phone);
                        }
                        if(res.billingAddressError.billing_address_1){
                             $('#error_billing_address_1').html(res.billingAddressError.billing_address_1);
                        }
                        if(res.billingAddressError.billing_city){
                             $('#error_billing_city').html(res.billingAddressError.billing_city);
                        }
                        if(res.billingAddressError.billing_state){
                             $('#error_billing_state').html(res.billingAddressError.billing_state);
                        }
                        if(res.billingAddressError.billing_pincode){
                             $('#error_billing_pincode').html(res.billingAddressError.billing_pincode);
                        }
                    }
                  },
                  error: function (xhr, ajaxOptions, thrownError) {
                    $("#rzp-button1").removeAttr("disabled");
                    showMessage('danger', `Something went wrong please try again`);
                  }
            });
        }); 

        function trigger_razorpay(options) {

            var rzp1 = new Razorpay(options);
            rzp1.open();
        }

        </script>
    <?php
       // }
    ?> 
    
    <?php 
        if ($this->router->fetch_class() == 'home' && $this->router->fetch_method() == 'registerBikeSuccess') { 
            redirect( base_url().'home/registerBikePdf', 'refresh' );
        }
    ?> 

    <!--Start of Tawk.to Script-->
    <script type="text/javascript">
    var Tawk_API=Tawk_API||{}, Tawk_LoadStart=new Date();
    (function(){
    var s1=document.createElement("script"),s0=document.getElementsByTagName("script")[0];
    s1.async=true;
    s1.src='https://embed.tawk.to/61bc7bdbc82c976b71c1e853/1fn447kvi';
    s1.charset='UTF-8';
    s1.setAttribute('crossorigin','*');
    s0.parentNode.insertBefore(s1,s0);
    })();
    </script>
    <!--End of Tawk.to Script-->
</body>

</html>